<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:media="using:Microsoft.UI.Xaml.Media"
                    >

    <Style x:Key="PageStyle"
           TargetType="Page"
           >
        <Setter Property="Background" Value="{ThemeResource ApplicationPageBackgroundThemeBrush}" />
    </Style>
    <Style x:Key="ApplicationLogoStyle"
           TargetType="Image"
           >
        <Setter Property="Source" Value="../Assets/AppLogo/62e914dea1e1b17f781f57f4.png" />
        <Setter Property="Height" Value="200" />
        <Setter Property="Width" Value="200" />
        <Setter Property="Stretch" Value="Uniform" />
    </Style>
    <media:AcrylicBrush x:Key="BgAcrylicBrushHome"
                        BackgroundSource="HostBackdrop"
                        FallbackColor="White"
                        TintColor="{ThemeResource SystemAccentColorLight3}"
                        TintLuminosityOpacity="0"
                        TintOpacity="0"
                        />

    <Style x:Key="HeaderTextBlockStyle"
           BasedOn="{StaticResource HeaderTextBlockStyle}"
           TargetType="TextBlock"
           >
        <Setter Property="FontFamily" Value="Arial" />
    </Style>
    <SolidColorBrush x:Key="ThemeControlForegroundBaseHighBrush"
                     Color="{ThemeResource SystemBaseHighColor}"
                     />

    <SolidColorBrush x:Key="SystemControlForegroundAccentBrush"
                     Color="#FFFFFFFF"
                     />
    <!--  Override foreground color for this custom theme  -->
    <SolidColorBrush x:Key="MTCScrubberBarTextBrush"
                     Color="#FF101010"
                     />

    <SolidColorBrush x:Key="HeaderColor"
                     Color="#10101010"
                     />

    <media:AcrylicBrush x:Key="bgAcrylicBrush"
                        BackgroundSource="HostBackdrop"
                        FallbackColor="{ThemeResource SystemAccentColorLight2}"
                        Opacity="1.00"
                        TintColor="#FF101010"
                        TintOpacity="0.8"
                        />
    <Style x:Key="HomePageBackground"
           TargetType="Page"
           >
        <Setter Property="Background" Value="{StaticResource BgAcrylicBrushHome}" />
    </Style>

    <Style x:Key="PageBackground"
           TargetType="Panel"
           >
        <Setter Property="Background" Value="{StaticResource bgAcrylicBrush}" />
    </Style>

</ResourceDictionary>
