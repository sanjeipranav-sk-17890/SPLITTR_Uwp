<Page
    x:Class="SPLITTR_Uwp.Views.ExpensesListAndDetailViewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SPLITTR_Uwp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:vm="using:SPLITTR_Uwp.ViewModel.Models"
    xmlns:header="using:SPLITTR_Uwp.ViewModel.Models.ExpenseListObject"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    Style="{ThemeResource HomePageBackground}"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    Loaded="ExpensesListAndDetailViewPage_OnLoaded">
    <Page.Resources>
        <CollectionViewSource  x:Name="CollectionViewSource" x:Key="GroupedSource" IsSourceGrouped="True"/>
        <Style x:Key="HideExpensesButtonStyle" x:FieldModifier="public" x:Name="HideExpenseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Foreground" Value="{ThemeResource SystemAccentColorDark1}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <FontIcon FontFamily="Segoe MDL2 Assets" VerticalAlignment="{TemplateBinding VerticalAlignment}"  Glyph="&#xE761;" Margin="2 2 4 0" FontSize="20"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="OpenPaneButtonStyle" x:FieldModifier="public" x:Name="OpenPaneButtonStyle" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <FontIcon FontFamily="Segoe MDL2 Assets" VerticalAlignment="{TemplateBinding VerticalAlignment}"  Glyph="&#xEA5B;" Margin="2 2 4 0" FontSize="20"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ClosePaneButtonStyle" x:FieldModifier="public" x:Name="ClosePaneButtonStyle" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <FontIcon FontFamily="Segoe MDL2 Assets" VerticalAlignment="{TemplateBinding VerticalAlignment}"  Glyph="&#xEA49;" Margin="2 2 4 0" FontSize="20"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ShowExpensesButtonStyle" x:Name="ShowExpenseButtonStyle" x:FieldModifier="public" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Foreground" Value="{ThemeResource SystemAccentColorDark1}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <FontIcon FontFamily="Segoe MDL2 Assets" VerticalAlignment="{TemplateBinding VerticalAlignment}"  Glyph="&#xE7B3;" Margin="2 2 4 0" FontSize="20"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>
   
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition MinWidth="400"></ColumnDefinition>
            <ColumnDefinition Width="10"></ColumnDefinition>
            <ColumnDefinition MinWidth="400"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" CornerRadius="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Orientation="Horizontal">
                
                <Button  x:Name="NavigationPaneControlButton" Click="NavigationPaneName_OnClick" Style="{StaticResource OpenPaneButtonStyle}">
                    
                </Button>

                <TextBlock Text="{x:Bind TitleText,Mode=OneWay}" VerticalAlignment="Center" FontWeight="Bold" Margin="4 0 0 0" FontSize="18"></TextBlock>
            </StackPanel>
            <ListView Grid.Row="1" x:Name="ExpensesLIstView"  ItemsSource="{x:Bind CollectionViewSource.View , Mode=OneWay}">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="MinHeight" Value="0"></Setter>
                        <Setter Property="HorizontalAlignment" Value="Stretch"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="vm:ExpenseViewModel">
                        <StackPanel Visibility="{x:Bind Visibility,Mode=OneWay}" x:Name="ExpenseItemGrid" >
                            <TextBlock Text="{x:Bind Note}" />
                            <TextBlock Text="Item TemplateComing Soon" />
                            <TextBlock  Text="{x:Bind ExpenseStatus}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
                <ListView.GroupStyle>
                    <GroupStyle>
                        <GroupStyle.HeaderTemplate>
                            <DataTemplate x:DataType="header:ExpenseGroupingList" >
                                <Grid HorizontalAlignment="Stretch" Width="{Binding ElementName=ExpensesLIstView,Path=ActualWidth,Mode=OneWay}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <StackPanel Orientation="Horizontal"  Grid.Column="0" Margin="10,0">
                                        <StackPanel.Resources>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="{ThemeResource SystemAccentColorDark1}"></Setter>
                                            </Style>
                                        </StackPanel.Resources>
                                        <Button Click="ExpenseShowButton_OnClick" Style="{ThemeResource ShowExpensesButtonStyle}">
                                        </Button>
                                        <TextBlock Text="{x:Bind GroupHeader.GroupName, Mode=OneWay}"   
                                                   FontWeight="Bold" />
                                        <TextBlock Text="(" Margin="5 0 0 0"/>
                                        <TextBlock Text="{x:Bind GroupHeader.NoOfExpenses,Mode=OneWay}" Margin="0"></TextBlock>
                                        <TextBlock Text=")"/>
                                    </StackPanel>
                                    <MenuFlyoutSeparator Grid.Row="1" HorizontalAlignment="Stretch" Margin="10 0 30 0"></MenuFlyoutSeparator>

                                </Grid>

                                <!---->

                            </DataTemplate>
                        </GroupStyle.HeaderTemplate>
                        <GroupStyle.HeaderContainerStyle>
                            <Style TargetType="ListViewHeaderItem">
                                <Setter Property="FontFamily" Value="{ThemeResource ContentControlThemeFontFamily}" />
                                <Setter Property="FontSize" Value="{ThemeResource ListViewHeaderItemThemeFontSize}" />
                                <Setter Property="Margin" Value="0" />
                                <Setter Property="Padding" Value="12,8,12,0" />
                                <Setter Property="HorizontalContentAlignment" Value="Left" />
                                <Setter Property="VerticalContentAlignment" Value="Center" />
                                <Setter Property="MinHeight" Value="{ThemeResource ListViewHeaderItemMinHeight}" />
                                <Setter Property="UseSystemFocusVisuals" Value="True" />
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListViewHeaderItem">
                                            <ContentPresenter x:Name="ContentPresenter"
                                                    Background="{TemplateBinding Background}"
                                                    Content="{TemplateBinding Content}"
                                                    ContentTemplate="{TemplateBinding ContentTemplate}"
                                                    ContentTransitions="{TemplateBinding ContentTransitions}"
                                                    HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                    VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}" />
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </GroupStyle.HeaderContainerStyle>
                    </GroupStyle>
                </ListView.GroupStyle>
            </ListView>

        </Grid>
        <!--Column Grid Splitter-->
        <controls:GridSplitter Grid.Column="1" Width="10" ResizeBehavior="BasedOnAlignment"
                               ResizeDirection="Auto" Background="White"  CornerRadius="5"  >
            <controls:GridSplitter.Element>
                <Grid Background="White" >

                </Grid>
            </controls:GridSplitter.Element>
        </controls:GridSplitter>
        <!-- ================================= -->
        <Grid Grid.Column="2" Background="DarkGray"  CornerRadius="15">
            <TextBlock FontSize="56" HorizontalAlignment="Center"  VerticalAlignment="Center"> coming soon</TextBlock>
        </Grid>

    </Grid>
</Page>
